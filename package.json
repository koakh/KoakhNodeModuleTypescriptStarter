{
  "name": "@koakh/typescript-node-module-starter",
  "version": "1.0.0",
  "author": "Mário Monteiro",
  "license": "ISC",
  "description": "",
  "main": "dist/index.js",
  "scripts": {
    "build": "tsc",
    "dev": "ts-node-dev --respawn -- src/index.ts",
    "--- patch": "patch/update readme, and inc version, if used with publish enters in bootLoop",
    "patch": "npm version patch",
    "--- pub": "don't use keyword publish",
    "pub": "npm publish --access public"
  },
  "devDependencies": {
    "@types/node": "^14.6.3",
    "ts-node-dev": "^1.0.0-pre.62",
    "typescript": "^4.0.2"
  }
}
